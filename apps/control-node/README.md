# @arduano/agent-multiplex-control-node

The reference protocol-v4 control-node executable. It combines the canonical
catalog and metadata authority, SQLite persistence, authenticated p2prpc links,
and optional local operator HTTP serving into one deployable Node.js process.

Run `agent-multiplex-control-node --help` after installation for configuration
and startup options.
