# @arduano/agent-multiplex-cli

The reference `agent-multiplex` command-line client for a protocol-v6 access
gateway. It supports listing and searching sessions, durable launch and archive
operations, metadata proposals, native agent control, and interactive event
watching.

Run `agent-multiplex --help` after installation for commands and configuration.
