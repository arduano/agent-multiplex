# @arduano/agent-multiplex-adapter-mock

A deterministic in-memory harness adapter for Agent Multiplex. It implements
the runtime adapter boundary without starting a real coding agent and is useful
for integration, topology, recovery, and fleet-scale tests.

It is test infrastructure, not a simulator of Codex or Copilot model behavior.
